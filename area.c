#include<stdio.h>
int main(){
    int a, b;
    printf("Enter length of a rectangle =",a);
    scanf("%d", &a);
    
    printf("Enter the the breadth of rectangle  =",b);
    scanf("%d", &b);
    int area = a * b;
    printf("Area of the Rectangle = %d", area);
    scanf("%d", &area);
    return 0;
}